// Keep empty when the video service is reverse-proxied at /api/video on this domain.
// For a separate service use an HTTPS sibling subdomain and configure WEB_ORIGINS there.
window.HAOWORD_VIDEO_API = '';
