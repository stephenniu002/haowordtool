import fs from 'node:fs';
import path from 'node:path';
import {fileURLToPath} from 'node:url';
import {translate} from '../assets/video-i18n.mjs';
import {zipFiles} from '../assets/app-builder-core.mjs';
const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..'),base='https://haowordtool.com';
const read=p=>fs.readFileSync(path.join(root,p),'utf8');
const write=(p,s)=>{fs.mkdirSync(path.dirname(path.join(root,p)),{recursive:true});fs.writeFileSync(path.join(root,p),s);};
const esc=s=>s.replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const alternates=(zh,en)=>`<link rel="alternate" hreflang="zh-CN" href="${base}${zh}"><link rel="alternate" hreflang="en" href="${base}${en}"><link rel="alternate" hreflang="x-default" href="${base}${en}">`;
const schema=obj=>`<script type="application/ld+json">${JSON.stringify(obj).replace(/</g,'\\u003c')}</script>`;
const copy={
 '每日计划 · 教程示例':'Daily plan · HTML learning example',
 '用于学习 HTML、CSS 和 JavaScript 导入、手机预览与 ZIP 导出的免费示例。':'A free daily-plan example for importing HTML, CSS and JavaScript, checking mobile layouts and exporting a ZIP.',
 '今天，从一件小事开始':'START WITH ONE SMALL THING',
 '我的每日计划':'My daily plan','把重要的事写下来，一件一件完成。':'Write down what matters. Finish one thing at a time.',
 '今日完成':'completed today','添加一件要做的事':'Add a task','例如：读书 20 分钟':'For example: read for 20 minutes','添加':'Add',
 '给今天留 20 分钟阅读':'Read for 20 minutes','出去散步，放松一下':'Go outside for a walk','完成一个小目标':'Finish one small goal',
 '这是学习示例。任务只在当前页面保存，刷新后重置。':'Learning example: tasks stay in this page only and reset on reload.'
};
let sample=read('examples/daily-plan/index.html');
for(const [zh,en]of Object.entries(copy))sample=sample.replaceAll(zh,en);
sample=sample.replace('lang="zh-CN"','lang="en"');
const exampleFiles={'index.html':sample,'styles.css':read('examples/daily-plan/styles.css'),'app.js':read('examples/daily-plan/app.js'),'README.txt':'Daily plan learning example. Extract the ZIP and open index.html. Keep HTML, CSS and JS in the same folder. No external dependencies. Tasks reset on reload. You may modify and reuse this sample.'};
for(const [name,data]of Object.entries(exampleFiles))write('examples/daily-plan-en/'+name,data);
write('examples/daily-plan-en.zip',Buffer.from(await zipFiles(exampleFiles).arrayBuffer()));

const lessons=[
{slug:'run-ai-html',title:'How to run AI-generated HTML, CSS and JavaScript',description:'Import a working daily-plan example, edit its heading and test real JavaScript interactions. Learn which AI code can run without a build step.',body:`
<p>You have received code from an AI assistant. Before pasting it into an editor, check what kind of project it is. A complete HTML document can run in a browser. A React component, a package.json file or a set of npm commands describes a different workflow. This lesson uses plain HTML, CSS and JavaScript with no external libraries.</p>
<h2>Start with a working example</h2><p>Download the daily-plan ZIP below and extract it. It contains index.html, styles.css, app.js and a README. The page has three tasks, checkboxes and an input for adding another task. It does not need an account or a server, and its tasks reset when you reload it.</p>
<ol><li>Open the English workspace and select <strong>Import tutorial example</strong>. This replaces the current editor contents, so export any project you want to keep first. Alternatively, use <strong>Import HTML / CSS / JS</strong> and select the three extracted code files together. Do not select the ZIP itself.</li><li>In the HTML tab, find <code>&lt;h1&gt;My daily plan&lt;/h1&gt;</code>. Change the text to <code>My study plan</code>. Keep the opening and closing tags.</li><li>Select the run button. The heading in the preview should now say My study plan.</li><li>Check the first task. The completed count should change from 0 / 3 to 1 / 3.</li><li>Enter <code>Review one lesson</code> in the task input and press Add. A fourth task should appear, and the count should read 1 / 4.</li></ol>
<h2>What each successful check tells you</h2><p>A visible heading shows that HTML loaded. Colors, spacing and rounded panels show that the stylesheet loaded. A changing counter and a newly added task demonstrate that JavaScript ran. Check those layers in that order. A screenshot of a page alone does not show whether its buttons work.</p>
<h2>When your own AI code does not run</h2><ul><li><strong>Raw code appears:</strong> remove Markdown fences such as three backticks. Save the file as index.html, not index.html.txt.</li><li><strong>The page has no styling:</strong> import the CSS file or paste its contents into the CSS tab. Run the project again.</li><li><strong>Buttons do nothing:</strong> import app.js into the JavaScript tab. Check that IDs used by the script still match the HTML. Renaming task-form in only one file breaks this example.</li><li><strong>The code imports React or another package:</strong> use that project's build instructions, or request a standalone vanilla HTML/CSS/JS version from your AI assistant.</li><li><strong>A remote image or API is missing:</strong> this workspace's preview blocks external network requests and dependencies. Test network-dependent code in an appropriately configured development environment.</li></ul>
<h2>Finish with one small change</h2><p>Edit the subtitle as well as the heading. Run the project and repeat both interaction checks. Your result should be a working page with your own text, not just untested code. Continue to the mobile lesson to check whether the same interface works at a narrow width.</p>`},
{slug:'preview-mobile-webpage',title:'How to preview a webpage at mobile width',description:'Use a 390px preview to test wrapping, forms and long task labels. Follow a responsive CSS example and learn what still needs a real phone test.',body:`
<p>A desktop preview can hide narrow-screen problems: an input pushes a button outside a panel, a heading gets clipped or a long task creates horizontal scrolling. Use the same daily-plan files from the first lesson so that your checks are repeatable. A narrow preview tests layout; it does not emulate a phone's keyboard, browser engine or performance.</p>
<h2>Prepare content that exposes problems</h2><p>Import the English tutorial example into the workspace. Change the heading to <code>My study and everyday life plan</code>. Add a task saying <code>Review this week's notes and write down three questions to practise next</code>. First verify that checking and adding tasks works in the desktop preview.</p>
<h2>Compare two widths</h2><ol><li>Find the preview-size selector beside the preview status and choose the mobile option. The workspace requests a 390px-wide preview.</li><li>Check that the long heading wraps inside the card. The input and Add button should stack vertically.</li><li>Read the entire long task. Its checkbox should remain visible and its label should wrap without crossing the card boundary.</li><li>Check one task and add another. A working initial layout is not enough: the layout must also handle changed content.</li><li>Switch back to the desktop option. With enough room, the input and button should return to a shared row.</li></ol>
<h2>The responsive rule used in the example</h2><pre><code>@media (max-width: 480px) {
  .app { margin: 12px; padding: 20px; }
  .entry { flex-direction: column; }
  .entry button { width: 100%; }
}</code></pre><p>At widths of 480px or less, this rule stacks the form controls. It gives the button its own row instead of letting it compete with the input. The example also uses border-box sizing so padding is included in element widths. Try changing the breakpoint to 420px, run the project again and compare the same content at desktop and mobile widths.</p>
<h2>A practical inspection checklist</h2><ul><li><strong>Reading:</strong> headings, instructions and long task labels are fully visible. Try a long unbroken word as a separate stress test.</li><li><strong>Interaction:</strong> click the checkbox label as well as the small checkbox. Use the keyboard to move focus through the form and confirm the focus indicator remains visible.</li><li><strong>Width:</strong> you can read the content without dragging the page sideways. Hiding overflow is not a substitute for fixing an oversized element.</li><li><strong>State:</strong> repeat the checks after adding several tasks and entering a long line of text.</li></ul>
<h2>What to check on a real device</h2><p>If the workspace itself is narrower than 390px, its parent container may further constrain the preview. The mobile option is therefore a layout aid, not a guarantee of an exact device simulation. After exporting and hosting the page, open it on a real phone. Check scrolling, font rendering, touch input and the on-screen keyboard. Repeat these checks if you later add images or network requests.</p>
<p>You are ready to export when both preview sizes let you add and complete tasks, long content stays within the card, and controls remain readable and usable.</p>`},
{slug:'export-html-css-js-zip',title:'How to export HTML, CSS and JavaScript as a working ZIP',description:'Export a static project, extract it into a fresh folder and verify its files and interactions. Understand the difference between source files, hosting and an App installer.',body:`
<p>A downloaded ZIP is useful when someone else can tell how to open it and what it can do. This lesson exports the daily-plan project, which uses local HTML, CSS and JavaScript. The result is source code. It is not an Android APK, an iPhone installation file or an automatically published website.</p>
<h2>Check the version before exporting</h2><p>Import the tutorial example and change its heading to My study plan. Run the project, check one task and add another. Export reads the current contents of all three editor tabs, so run again after editing. Otherwise, the code you download may differ from the last preview you inspected.</p>
<h2>Export, extract and open</h2><ol><li>Choose the workspace's project ZIP download button. It generates <code>haoword-app-project.zip</code>. Your browser decides where to save it.</li><li>Locate the ZIP and extract it into a new folder. Do not test by opening just one file inside an archive viewer.</li><li>Keep index.html, styles.css and app.js in the same directory.</li><li>Open index.html in a browser. Its heading should match the version you exported, and its colors and spacing should be present.</li><li>Check a task and add a new task. Compare the behavior with the workspace, then make the browser narrow to check the responsive layout.</li></ol>
<h2>What is in the project export?</h2><table><thead><tr><th>File</th><th>Purpose</th></tr></thead><tbody><tr><td>index.html</td><td>The page entry point: headings, form and task list.</td></tr><tr><td>styles.css</td><td>Colors, spacing and mobile layout rules.</td></tr><tr><td>app.js</td><td>Task creation and completion counting.</td></tr><tr><td>brief.json</td><td>The requirements you entered in the workspace's brief form.</td></tr><tr><td>README.txt</td><td>Opening instructions and the scope of the export.</td></tr></tbody></table><p>The downloadable tutorial sample has four files; the workspace export additionally includes brief.json. During export, the workspace points the page to the same-folder stylesheet and script. It does not automatically package extra images, fonts, external libraries or a backend.</p>
<h2>Fix differences after download</h2><ul><li><strong>Missing styles:</strong> check that styles.css was extracted with index.html and that you did not rename it.</li><li><strong>Missing interactions:</strong> confirm app.js is in the same folder and inspect any filename or path changes.</li><li><strong>Your own project depends on fetch or modules:</strong> unlike this dependency-free example, it may require an HTTP server, a build step or a configured backend. Opening a file directly is not a universal launch method.</li><li><strong>No download appears:</strong> check the browser's download panel and permissions before generating repeated copies.</li></ul>
<h2>Before sharing the folder</h2><p>Test from a fresh extraction rather than your editing folder. Review both the code and brief.json for private information, passwords or API keys. Include an honest note: this is a static demo, tasks reset on reload, and there is no account, payment or cloud storage service. To share a public link, separately deploy the public files to a website host.</p>
<p>Success means you can open the extracted project independently, see the expected styling and heading, add and complete tasks, and use the narrow layout. The presence of a ZIP file alone is not the final check.</p>`}
];
const downloads=`<section class="lesson-download"><h2>Use the same working example</h2><p>Free to modify. No external libraries; tasks reset on reload.</p><div class="actions"><a class="button primary" href="/examples/daily-plan-en.zip" download>Download sample ZIP</a><a class="button" href="/en/?utm_source=tutorial&amp;utm_medium=internal&amp;utm_campaign=english_seo">Open workspace →</a><a class="button" href="/examples/daily-plan-en/">Try the standalone example</a></div><p>In the workspace, select <strong>Import tutorial example</strong> to load the files. Export existing work first.</p></section>`;
const logo='<img src="/assets/hao-logo.svg?v=4" alt="HaoWordStudio" width="236" height="46" style="display:block;width:min(236px,50vw);height:auto">';
function lessonPage(title,description,url,zh,body){return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${esc(title)} | HaoWord Studio</title><meta name="description" content="${esc(description)}"><link rel="canonical" href="${base}${url}">${alternates(zh,url)}<meta property="og:title" content="${esc(title)}"><meta property="og:description" content="${esc(description)}"><meta property="og:url" content="${base}${url}"><meta property="og:type" content="article"><link rel="stylesheet" href="/assets/studio-design.css?v=seo1"><link rel="stylesheet" href="/assets/tutorials.css"></head><body><header class="site-top"><a class="logo" href="/en/">${logo}</a><nav aria-label="Main navigation"><a href="/en/">Workspace</a><a href="/en/learn/">Tutorials</a><a href="${zh}" lang="zh-CN">中文</a></nav></header><main class="lesson">${body}</main><footer class="site-footer"><span>HaoWord Studio · Practical tutorials</span><nav><a href="/privacy.html">Privacy</a><a href="/contact.html">Contact</a></nav></footer></body></html>`;}
for(const t of lessons){const url='/en/learn/'+t.slug+'.html',zh='/learn/'+t.slug+'.html';
 const structured={'@context':'https://schema.org','@type':'TechArticle',headline:t.title,description:t.description,inLanguage:'en',dateModified:'2026-09-07',author:{'@type':'Organization',name:'HaoWord Studio',url:base},mainEntityOfPage:base+url};
 write(url.slice(1),lessonPage(t.title,t.description,url,zh,`<p class="eyebrow">PRACTICAL GUIDE · UPDATED SEPTEMBER 7, 2026</p><h1>${t.title}</h1><p class="lesson-lead">${t.description}</p><article>${t.body}</article>${downloads}<h2>Continue practising</h2><ul>${lessons.filter(x=>x!==t).map(x=>`<li><a href="/en/learn/${x.slug}.html">${x.title}</a></li>`).join('')}</ul><p class="hint">Written by HaoWord Studio for the current workspace and included sample.</p>${schema(structured)}`));
 let original=read(zh.slice(1));original=original.replace('</head>',alternates(zh,url)+'</head>').replace('<main class="lesson">',`<main class="lesson"><p><a href="${url}" lang="en">Read this tutorial in English →</a></p>`);write(zh.slice(1),original);
}
write('en/learn/index.html',lessonPage('Learn to run, preview and export a static web project','Three hands-on HTML, CSS and JavaScript tutorials with a shared, downloadable daily-plan example.','/en/learn/','/learn/',`<p class="eyebrow">LEARN BY BUILDING</p><h1>Turn your code into a working page.</h1><p class="lesson-lead">Start with one small project. Edit it, test its mobile layout and export files that work outside the editor.</p>${lessons.map((t,i)=>`<article class="lesson-card"><small>LESSON ${i+1}</small><h2><a href="/en/learn/${t.slug}.html">${t.title}</a></h2><p>${t.description}</p></article>`).join('')}${downloads}`));
{let page=read('en/learn/index.html');page=page.replace('</main>','<section class="lesson-download"><h2>Watch the 45-second demonstration</h2><video controls playsinline preload="none" poster="/marketing/demo-en/cover.jpg" style="width:100%;max-height:600px;background:#edf3e7"><source src="/marketing/demo-en/haoword-english-45s.mp4" type="video/mp4"></video><p>Actual interface screenshots edited into a captioned demonstration. No narration.</p><a href="/marketing/demo-en/haoword-english-45s.mp4" download>Download the video</a></section></main>');write('en/learn/index.html',page);}
let hub=read('learn/index.html');hub=hub.replace('</head>',alternates('/learn/','/en/learn/')+'</head>').replace('<main class="lesson">','<main class="lesson"><p><a href="/en/learn/" lang="en">English tutorials →</a></p>');write('learn/index.html',hub);

// The English route serves the actual editor as static HTML, not a language-cookie redirect.
let english=read('index.html');
english=english.replace(/(<span\b[^>]*data-en="([^"]*)"[^>]*>)[^<]*(<\/span>)/g,(_,open,en,close)=>open+en+close);
english=english.split(/(<[^>]+>)/g).map(part=>part.startsWith('<')?part.replace(/(placeholder|aria-label|title)="([^"]*)"/g,(_,attr,value)=>`${attr}="${esc(translate(value,'en'))}"`):part.trim()?part.replace(part.trim(),esc(translate(part.trim(),'en'))):part).join('');
const title='Online HTML, CSS & JavaScript Editor | HaoWord Studio',description='Run static HTML, CSS and JavaScript, preview mobile layouts and export a ZIP. Free browser workspace with step-by-step tutorials and a working sample.';
english=english.replace('<html lang="zh-CN"','<html lang="en"').replace(/<title>[^<]*<\/title>/,`<title>${esc(title)}</title>`).replace(/<meta name="description" content="[^"]*">/,`<meta name="description" content="${description}">`).replace(/<meta property="og:title" content="[^"]*">/,`<meta property="og:title" content="${esc(title)}">`).replace(/<meta property="og:description" content="[^"]*">/,`<meta property="og:description" content="${description}">`).replace(/<meta property="og:url" content="[^"]*">/,`<meta property="og:url" content="${base}/en/">`).replace(/<link rel="canonical" href="[^"]*">/,`<link rel="canonical" href="${base}/en/">`).replaceAll('href="/learn/"','href="/en/learn/"').replaceAll('href="/video-studio.html"','href="/video-studio/en/"').replaceAll('href="/"','href="/en/"').replace('value="我的新 App"','value="My new app"').replace('<option value="en">','<option value="en" selected>').replace('title="替换当前编辑器，请先导出要保留的代码"','title="Replaces the editor contents. Export any work you want to keep first."');
english=english.replace('</head>',alternates('/','/en/')+'</head>').replace('</main>','<section class="lesson-download"><h2>Build with a complete example</h2><p>Run a daily task list, check a 390px layout and open your exported files in a browser. The three tutorials include success checks and troubleshooting.</p><a class="button" href="/en/learn/">Start the practical tutorials →</a><p>This editor runs static frontend code. AI generation, live backend services and signed mobile installers are not included.</p></section></main>').replace('</footer>','<a href="/" lang="zh-CN">中文</a></footer>');
english=english.replaceAll('Your next app starts here.','Run HTML, CSS and JavaScript.');
write('en/index.html',english);
for(const file of ['index.html','app-builder.html']){let original=read(file);original=original.replace('</head>',alternates('/','/en/')+'</head>').replace('</footer>','<a href="/en/" lang="en">English workspace</a></footer>');write(file,original);}
let sitemap=read('sitemap.xml');for(const url of ['/en/','/en/learn/',...lessons.map(t=>'/en/learn/'+t.slug+'.html')])if(!sitemap.includes(`<loc>${base}${url}</loc>`))sitemap=sitemap.replace('</urlset>',`<url><loc>${base}${url}</loc><lastmod>2026-09-07</lastmod></url></urlset>`);write('sitemap.xml',sitemap);
console.log('English editor, three practical tutorials, sample ZIP and reciprocal language links generated.');
