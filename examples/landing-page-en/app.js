const form=document.querySelector('#interest-form');const email=document.querySelector('#email');const message=document.querySelector('#form-message');form.addEventListener('submit',event=>{event.preventDefault();if(!email.validity.valid){message.textContent='Enter a valid email address to test the form.';email.focus();return;}message.textContent='Validation works. This demo did not send or store the address.';form.reset();});